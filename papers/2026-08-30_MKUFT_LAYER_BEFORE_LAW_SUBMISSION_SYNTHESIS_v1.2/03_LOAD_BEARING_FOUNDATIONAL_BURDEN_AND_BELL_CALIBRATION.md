# 7. Load-bearing relations, independent content, and strongest-fair-null control

## 7.1 Relation ablation

```math
\lambda_i(B)=K_L(B)-K_L\!\left(B\setminus\{b_i\}\right)
```

For a declared capability K_L and candidate relation set B, lambda_i(B)
measures the change produced by removing relation b_i. In the later
future-sufficiency formalism, exact relation ablation is a predeclared
lawful protocol, not an automatic physical intervention. When the exact
comparator is unavailable or ambiguous, the correct verdict is unknown
rather than non-load-bearing.

## 7.2 Independent information-content test

```math
H_0:\;Y_{t+\Delta}\perp\!\!\!\perp I_t\mid X_t,H_t
```

Let X_t be the measured physical/environmental state, H_t measured
history, I_t a candidate informational variable, and Y\_(t+Delta) a
preregistered physical outcome. Independent I -\> P content is not
earned merely because I_t predicts something. It must add held-out or
interventional value beyond the strongest adequate physical account,
survive relation-preserving re-encoding, and remain compatible with
conservation, no-signalling, accepted-limit recovery, and replication.

## 7.3 Strongest fair null

```math
F_{\mathrm{null}}^*=\max\{F_A^*,F_B^*,F_{\mathrm{ind}}^*,F_{A\to B}^*,F_{B\to A}^*,F_{\mathrm{replay}}^*\},
```

```math
G_{\mathrm{syn}}^*=F_{AB}-F_{\mathrm{null}}^*
```

The controlling null is the strongest commensurable ordinary alternative
under a matched resource envelope, not the weakest baseline that allows
a favoured condition to win. If complete-history replay matches the live
condition, live path-specific emergence is not earned. If serious
alternative pairings match the focal pair, relationship specificity is
not earned. If a simpler established model closes the same burden at
equal or lower complexity, the stronger interpretation contracts.

## 7.4 Equation-status discipline

| **Class** | **Meaning**                   | **Required burden**                                                                                                           |
|-----------|-------------------------------|-------------------------------------------------------------------------------------------------------------------------------|
| E0        | Notation / bookkeeping        | Typed addresses, tuples, projections, comparison labels.                                                                      |
| E1        | Research scaffold             | Mathematically legal candidate form without physical derivation or validation.                                                |
| E2        | Operational statistical model | Measured variables, frozen estimation, uncertainty, explicit baselines.                                                       |
| E3        | Replicated empirical model    | Held-out and independently replicated support for the tested relation.                                                        |
| E4        | Derived physical mechanism    | Defined physical states/couplings, symmetries, conservation, accepted-limit recovery, discriminating quantitative prediction. |

**No current foundational MKUFT equation is classified as E4.**

# 8. Foundational physics burden

The architecture becomes a physical theory only when Q, C_P, and L_P are
instantiated in concrete regimes. The following are acceptance
requirements, not results already obtained.

## 8.1 Entanglement and Bell structure

```math
S_{\mathrm{CHSH}}=E(a,b)+E(a,b')+E(a',b)-E(a',b')
```

For a bipartite experiment, a candidate must produce a valid joint
distribution P(A,B | a,b), recover tested quantum correlations, and
preserve observed no-signalling. A relational label that only
redescribes the standard non-separable joint state is not a new
mechanism.

## 8.2 Quantum and quantum-field-theory recovery

A successful foundational instantiation must recover the Born rule,
experimentally adequate dynamics, composition and tensor-product
structure where applicable, identical-particle statistics, relativistic
causal structure, and tested QFT limits. An architecture that reproduces
only a qualitative language of possibility and constraint has not met
this burden.

## 8.3 Gravitational recovery

The gravitational branch must recover Einstein dynamics or an
experimentally equivalent limit, including tested weak-field,
post-Newtonian, lensing, timing, gravitational-wave, and conservation
behaviour. Constraint and boundary structures already exist in general
relativity and effective field theory; redescribing them as
admissibility does not constitute a new gravitational mechanism.

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th><p><strong>Strongest physical null</strong></p>
<p>The null is not "no relation". It is the strongest adequate
physical-state, history, boundary, environment, and measurement-context
model. If that model determines the same law object and predictions, no
independent cross-layer physical cause has been earned.</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

# 9. Hostile calibration: an exact Bell chart that was allowed to fail

The Cross-Domain Compositional Schema v0.4 provides an adversarial
calibration because an initially attractive geometric construction was
carried far enough to be either earned or killed by native Bell
mathematics.

```math
\nu=c^{\mathsf T}E-2,\qquad
\Pi_c(E)=E-\frac{\nu}{4}c,\qquad
\lambda=V^{-1}\Pi_c(E),
```

```math
E=V\lambda+\frac{\nu}{4}c,\qquad \mathbf{1}^{\mathsf T}\lambda=1
```

For a selected CHSH facet orientation c, the tetrahedral facet carries
three independent affine coordinates and nu carries the fourth
transverse coordinate. The four correlators are reconstructed exactly.
On the local CHSH facet, nu = 0 and lambda reduces to ordinary
barycentric coordinates. Off the facet, the chart remains invertible
even when the projected coordinates leave the simplex.

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th><p><strong>Temptation</strong></p>
<p>At this point the geometry is exact, elegant, invertible, and
apparently tailored to the four-correlator object. This is precisely
where a weak framework could mistake a useful coordinate system for new
physics.</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

## 9.1 Native-owner squeeze

The natural four-dimensional simplex volume generated by the CHSH facet
and an off-facet point is

```math
\mathcal{V}_c(E)=\frac{|S_c(E)-2|}{3}
```

This quantity collapses to a rescaling of the already-known CHSH excess.
Because the chart is an invertible reparameterisation, the chart alone
cannot be new physics. The v0.4 physical residual is therefore NULL: no
new Bell inequality, no independent Tsirelson derivation, no quantum
mechanism, and no measurable physical delta survive native-owner
subtraction. Elegance, unfortunately for the candidate invariant, is not an independent observable.

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th><p><strong>Methodological result</strong></p>
<p>The architecture is intended to survive a mature domain telling it
"no". The retained result is the exact representation boundary and the
preserve-or-reopen lesson: the correlator chart closes its declared
target, while full Bell behaviour still contains marginal information
that must be reopened when the wider question depends on it.</p></th>
</tr>
</thead>
<tbody>
</tbody>
</table>

## 9.2 Public computational verification package

To make the Bell calibration independently reproducible without
requiring the reader to reconstruct the surrounding repository, this
revised synthesis publishes a compact computational verification
package. It contains the exact input orientation and symmetric Tsirelson
correlator, the derived CHSH value, transverse coordinate, facet
projection, barycentric coordinates, exact reconstruction check, and the
candidate four-volume, together with a standard-library Python script
that recomputes each value and exits non-zero if a declared identity
fails.

Public package:
https://github.com/mark45cdo-mkuft/MKUFT/tree/main/reproducibility/MKUFT_PREPRINTS_v1.2_BELL_CHSH_CALIBRATION

The package is deliberately typed as computational verification data. It
is original data generated for this revised manuscript in the limited
reproducibility sense that the numerical verification table and captured
outputs are newly generated from the declared equations. It is not an
experimental dataset, does not add an independent evidential increment
to Bell physics, and does not alter the null new-physics residual. Its
purpose is to make the calculation directly inspectable, rerunnable, and
checksum-addressable.
